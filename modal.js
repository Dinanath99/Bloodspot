// const thumbnailArr = [...document.querySelectorAll('.thumbnail')];
// const modalArr = [...document.querySelectorAll('.modal')];
// thumbnailArr.forEach((thumbnail, index) => {
//     thumbnail.addEventListener('click', () => {
//         modalArr[index].showModal();
//     })
// })

// console.log(thumbnailArr, modalArr);